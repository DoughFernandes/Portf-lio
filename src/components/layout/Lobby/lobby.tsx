import style from "./lobby.module.scss";

export default function Lobby (){
  return (
    <section id="lobby" className={style["container-lobby"]}>
      Lobby para minha apresentação
    </section>
  )
};
